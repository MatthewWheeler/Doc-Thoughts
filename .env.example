# Twilio credentials (get from twilio.com/console)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM=+15551234567

# Your phone number (owner of Doc) — used by seeder to register Doc
DOC_OWNER_PHONE=+15559876543

# Production webhook URL for Twilio signature validation (leave blank in dev)
WEBHOOK_URL=

# Server
PORT=3000

# SQLite file path (use /app/data/thoughts.db on Railway)
DB_PATH=./data/thoughts.db

# Send window: minutes after midnight (480 = 8am, range 720 = 12h window → 8am–8pm)
SEND_WINDOW_START=480
SEND_WINDOW_RANGE=720
